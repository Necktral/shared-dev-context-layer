export class ContextPresenter {
  // Slice 2: present loaded context in dedicated UI surface.
  public present(): never {
    throw new Error("ContextPresenter is not implemented in Slice 0/1.");
  }
}
