export class HandoffBuilder {
  // Slice 2: build structured handoff payload for downstream agents.
  public build(): never {
    throw new Error("HandoffBuilder is not implemented in Slice 0/1.");
  }
}
